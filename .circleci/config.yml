version: 2.1
orbs:
 node: circleci/node@1.1.6
jobs:
 build-and-test:
#  working_directory: ~/ask2020-node-react-cicd/server/
  executor: 
   name: node/default
  steps:
   - checkout
#   - node/install-packages
   - node/with-cache:
    steps:   
     - run: npm install
     - run: npm test
workflows:
 build-and-test:
  jobs:
   - build-and-test
#    filters:
#     branches:
#      ignore:
#       - dev
