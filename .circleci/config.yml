version: 2.1
orbs:
 node: circleci/node@2.1.0
jobs:
 build-and-test:
  working_directory: home/ask2020-node-react-cicd/server/
  executor: node/default
  steps:
   - checkout
#   - node/install-packages
   - run: npm install
   - run: npm test
workflows:
 build-and-test:
  jobs:
   - build-and-test
#    filters:
#     branches:
#      ignore:
#       - dev
